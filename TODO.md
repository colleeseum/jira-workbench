TODO:
- Bug: a staged `shadow comment` (never pushed) disappeared on its own between sessions -- observed on PLAT-9429: staged a comment, it showed up fine in `shadow diff`, then some time later (no `shadow push` ever ran) `shadow diff` reported "no local shadow changes" and the issue's cached component directory had also moved (`storage` -> `_unassigned`), implying some other command re-synced/refreshed that issue's full local cache in the background and wiped the pending shadow as a side effect. Did not fully root-cause which code path did this -- it's not `push_key`'s `baseUpdated` staleness check (`shadow.py:737`), since no push ever happened. Whatever it is, it defeats the entire point of "stage now, review/push later": a user (or an agent instructed to only stage, never push) can lose staged work silently, with no warning. Needs investigation into what refreshes a single issue's cache outside of `shadow push`'s own post-push refresh, and that path should preserve (or explicitly warn about) any pending shadow instead of silently dropping it.
- Support creating a new issue as a local-only shadow draft, staged for a later `shadow push`, instead of `issue create` always hitting the live Jira API the moment `--dry-run` is omitted. Make shadow-only the *default* behavior; require an explicit flag (e.g. `--push`) to create live immediately. Currently `issue create` has only two modes: `--dry-run` (prints resolved fields, no artifact at all) or a real create against the live API right away -- there's no staged-but-not-yet-real state for a brand-new issue the way `shadow set`/`comment`/`status-change` already provide for edits to an existing issue. Open design question: a shadow draft has no real Jira key yet (the server assigns it), so it needs some local placeholder identity (and a place to show up in `shadow status`/`diff`/`report`) until it's actually pushed and gets its real key back.
